version: "3.9"

services:
  ros-flir:
    build:
      dockerfile: Dockerfile.orin
    image: ros-devices:latest
    container_name: ros-devices
    network_mode: host
    restart: unless-stopped
    volumes:
      - /mnt/burak2/ros-device-control/bags:/app/bags
