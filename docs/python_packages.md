Here are packages that we have added to the system (installed via apt):

- python3-paho-mqtt (mqtt client for sending data. Will possibly be replaced in future)
- python3-zstandard (zstd api for python. Used to compress data.)
