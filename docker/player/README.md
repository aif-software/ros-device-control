# Bag player

Run `chmod +x select_bag.sh`. 

Run `./select_bag.sh` and select a bag that gets passed as an environment variable to the container. The container sets up foxglove bridge and starts playing the passed bag.
