version: "3.9"

services:
  ros-multisense:
    build:
      dockerfile: Dockerfile.multisense
    image: ros-multisense:latest
    container_name: ros-multisense
    network_mode: host
    restart: unless-stopped
